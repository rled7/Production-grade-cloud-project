# Benchmark Results

**Status: stub — populate after running the load + chaos suite against a live deployment.**

This document is generated by:

1. Deploying the stack (`terraform apply` against `terraform/environments/prod`).
2. Pushing images for all four languages to ECR (the CI/CD pipeline handles this on push to `main`).
3. Running:
   ```bash
   BASE_URL=https://<your-domain> ./benchmark/run_tests.sh
   BASE_URL=https://<your-domain> ./benchmark/chaos_test.sh
   ```
4. Pasting the `summary-*.csv` row contents into the **Load Test Results** section below.

---

## Load Test Results

| Language   | VUs | Duration | Reqs   | Req/sec | p95 (ms) | p99 (ms) | Error rate |
|------------|----:|---------:|-------:|--------:|---------:|---------:|-----------:|
| JavaScript |   ? |        ? |      ? |       ? |        ? |        ? |          ? |
| Python     |   ? |        ? |      ? |       ? |        ? |        ? |          ? |
| C          |   ? |        ? |      ? |       ? |        ? |        ? |          ? |
| C++        |   ? |        ? |      ? |       ? |        ? |        ? |          ? |

## Chaos Suite Results

| Probe                          | Expected | JS | Python | C | C++ |
|--------------------------------|----------|----|--------|---|-----|
| Malformed JSON                 | 400      | ?  | ?      | ? | ?   |
| Missing `content` field        | 400      | ?  | ?      | ? | ?   |
| Empty content                  | 400      | ?  | ?      | ? | ?   |
| Oversized payload (2 MiB)      | 413/400  | ?  | ?      | ? | ?   |
| Non-integer id                 | 400      | ?  | ?      | ? | ?   |
| Negative id                    | 400      | ?  | ?      | ? | ?   |
| SQL-injection probe            | 403/400  | ?  | ?      | ? | ?   |
| WAF rate limit trigger         | 403 wave | ?  | ?      | ? | ?   |
| `/health` still 200 after abuse| 200      | ?  | ?      | ? | ?   |

## Cache Fallback Behavior

Disable the Redis security-group inbound rule (or set the ElastiCache cluster
to a non-routable endpoint) for ~5 minutes while replaying the load test and
record:

- p95 / p99 latency change (expected: increased for cache-eligible reads,
  every read now hits Postgres).
- HTTP error rate (expected: **0**. The contract guarantees reads keep
  returning 200 with `"source":"db"` rather than 5xx.)

## Analysis

_(After running the suite — discuss observations.)_

- Which language sustained the highest throughput at the configured CPU/memory budget?
- How did latency compare between the compiled (C, C++) and interpreted (JS, Python) implementations under cache-hit vs cache-miss conditions?
- Did the Redis fallback work cleanly for all four implementations?
- Were any WAF rules tripped by legitimate-looking traffic (false positives)?

## Verdict

_(Declare a winner once data is in hand. The contract is identical across all
four implementations, so the differences here measure runtime/framework
characteristics rather than algorithmic choices.)_
